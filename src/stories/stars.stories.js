import { Stars } from "../three";

export default {
  title: "three/Stars",
  component: Stars,
};

const Template = (args) => <Stars {...args} />;

export const Points = Template.bind({});
Points.args = {};
